# Linux Enumeration

## Basic Commands

```shell
id
uname -a
cat /etc/issue
```

## Networking

```shell
ifconfig
ip a
netstat -anp
```

## Files & Permissions

```shell
find / -perm -u=s -type f 2>/dev/null
find / -name id_rsa
```
